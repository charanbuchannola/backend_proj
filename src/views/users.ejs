<!DOCTYPE html>
<html>
<head>
    <title>Users</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen p-8">
    <div class="max-w-2xl mx-auto">
        <h1 class="text-3xl font-bold mb-8 text-center">Users</h1>
        
        <div class="space-y-4">
            <% users.forEach(user => { %>
                <div class="bg-black p-4 rounded-lg flex items-center justify-between">
                    <div class="flex items-center space-x-3">
                        <img src="<%= user.image %>" 
                             alt="<%= user.username %>'s profile" 
                             class="w-10 h-10 rounded-full object-cover border-2 border-red-500">
                        <span class="text-white font-medium"><%= user.username %></span>
                    </div>
                    <a href="/users/profile/<%= user._id %>" 
                       class="bg-white text-black px-4 py-1 rounded-full text-sm hover:bg-gray-100 transition-colors">
                        Details
                    </a>
                </div>
            <% }); %>
        </div>
        
    </div>
</body>
</html>